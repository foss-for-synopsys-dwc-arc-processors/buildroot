#! /bin/sh

echo "[1] 32-bit debug of 'test'"
echo "[2] 64-bit debug of 'test'"

read -n 1 -p "what's it gonna be sailor? " reply
echo
choice=${reply:-9}
case ${choice} in
  [1])
    gdbserver.32 :1337 test
    ;;
  [2])
    gdbserver.64 :1338 test
    ;;
  *)
    ;;
esac
